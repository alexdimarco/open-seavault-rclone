# Reviewer-accepted mechanical edits to pre-existing tests, exempted from the Z1
# diff guard (cmd/seavault/z1_diff_guard_test.go). One per line:
#   <repo path><TAB><reason, with the phase, date, and what was checked>
# Adding a line here is itself a reviewed diff; keep the reason specific enough
# that a later reader can re-verify it.
cmd/seavault/main_test.go	U3 (2026-09-08): ensureLoopbackBind gained tlsOn/selfSigned parameters and a warnings return; the three TestEnsureLoopbackBind call sites thread (false,false) and discard the warnings; assertions unchanged (3 before, 3 after); overseer-checked against main.
cmd/seavault/commands_test.go	U3 (2026-09-08): H4 (TestRegistryEqualsDispatchBothDirections) rewritten to derive BOTH the registry side and the dispatch side from the code instead of a hardcoded command list, so a new command needs no test edit; proven red in both directions (tls rows unregistered; stray exec function); maintainer-authorized.
cmd/seavault/commands_test.go	U4 (2026-09-08): CLI-1 (I-P1, design §8 Z1 sanctioned "old non-2 exit code for an unknown subcommand"). TestExitCodeContract's three unknown-subcommand rows (rclone/remote/profile frobnicate) changed from want=1 to want=2 because dispatchGroup now returns exit 2 for an unknown subcommand, matching the unknown top-level command. ONLY the three want-values changed; the argv rows and the assertion body are unchanged; not loosened (still asserts an exact exit code). Proven red-first: with the dispatchGroup change reverted the three rows fail at commands_test.go:444 with "exit = 1, want 2".
cmd/seavault/tls_u3_test.go	U4 (2026-09-08): TestNoPreU3TestDeletedOrEdited now ALSO treats every path in this file as a guard-host file, so the two pre-phase-edit guards share one reviewed source of waivers instead of a hardcoded map that could not be extended without editing itself; the no-removal rule is unchanged; maintainer-authorized (the exemption-list mechanism approved 2026-09-08).
internal/setup/tlswizard_fixes_u3_test.go	U4 (2026-09-08): row A1-c6 (design §8, dca9122): the Tailscale renewal recipe becomes MONTHLY, so TestShowRenewalSubstitutesConcreteValues gains a per-row wantCron and the Tailscale row's pinned cron changes from "17 3 * * *" to "17 3 1 * *"; the lego row's value is byte-identical to before; the exact-substring assertion and every other check are preserved; proven red-first at tlswizard_fixes_u3_test.go:352 with the cadence reverted.
cmd/seavault/tls_fixes_u3_test.go	U4 (2026-09-08): polish-behaviour-1 (A3-c4) added a certConfigured bool to ensureLoopbackBind; the three call sites here thread the no-cert case (append `, false`), assertions and row tables unchanged; mechanical signature adaptation, overseer-checked.
cmd/seavault/tls_fixerc_u3_test.go	U4 (2026-09-08): polish-behaviour-1 (A3-c4) threads certConfigured into ensureLoopbackBind; the existing TestBindRefusalLeadsWithTLSRoute call site appends `, false` (unchanged behaviour) and the test GAINS a cert-configured sub-table asserting the refusal leads with `--tls`/"configured certificate"; no pre-U3 assertion loosened or removed; sanctioned by the assigned finding.
internal/webui/u4_polish_gui_test.go	U5 merge (2026-09-09): TestU4GUIOwn4ReadbackNamesFirstDifferingWord deflaked — its leak guard tripped ~1/190 runs when the random correct word 7 was one of the message's own fixed English words that are also BIP-39 entries (word, first, that, match, order, phrase, secret, security, control, unknown, neither). The guard now learns the fixed vocabulary by rendering the message once and flags only a non-fixed token equal to the correct word, with a self-check proving it is live; the token cleaner strips every non-letter so a parenthesized leak "(alpha)." is caught (the old scan missed it). STRENGTHENED, not loosened: proven red by injecting exactly that leak into the server, green 40x restored. Overseer edit.
