# Pre-code design review — Phase U1: the setup wizard

**What was reviewed:** `docs/design-setup-wizard.md` (Revision 1, pre-code) — a UI-agnostic `internal/setup` package (detect / plan / execute), a `seavault setup` CLI command with `--expert` and `--preset` forms, and a GUI first-run stepper.
**Repository / branch:** `open-seavault-rclone` @ `feature/setup-wizard`.
**Process run:** 10-lens pre-code design review (assurance-kit `process/design-review.md`): ten lens agents → skeptic pass → rescue pass → synthesis.
**Synthesis agent model:** claude-opus-4-8.

## Verdict: GO_WITH_CONDITIONS

No blocker survived both the skeptic and the rescue pass, so this is not a NO_GO. Everything that survived the skeptic is dischargeable by a Revision-2 design change applied before build. The rescue pass named no mechanism changes because no finding reached blocker status. Three independently re-verified facts anchor the material conditions: (1) `serveNoSession` returns **403**, not the 401 T6 asserts (server.go:742); (2) `docs/cloud-provider-notes.md` contains **no** caveat text to "lift from"; (3) `remote config create` writes only a header comment (remotes.go:323), so the rclone branch reaches no real backend from nothing.

The 14 numbered conditions are filed in full at `docs/review-setup-wizard-predesign.md`, each tagged with the findings it discharges and the test-matrix row the build must add or re-spec. The design is sound in shape (real shared `internal/setup` seam; reused KDF floor, recovery ceremony, keychain, profile, and session-auth primitives); the survivors are unbuilt-detail gaps, not architectural defects.

**Rescue outcome:** `{"rescues":[],"overall":"no blockers survived the skeptic pass; rescue not needed"}`.

The complete per-lens findings table, the refuted list with refuting quotes, and the conditions→test-row mapping are in the filed report at `docs/review-setup-wizard-predesign.md`.

## Conditions (authoritative list, from the synthesis structured output)

| # | Condition | Addresses |
|---|---|---|
| C1 | Close the rclone-branch credential dead-end. RemoteAdd writes only a pointer profile and `remote config create` writes only a header comment (remotes.go:323), so RemoteTest fails for every real backend from nothing. Either add an rclone-config step (reuse `seavault remote config create/import`, drive `rclone config`, provision the SFTP SSH key) BEFORE RemoteTest, or scope U1's rclone path to 'import an existing rclone remote' and say so in §2/§3.3. Never report 'synced' on a failing RemoteTest. | purpose-threat-fit-2, usability-friction-1, integration-seams-1 |
| C2 | Give the synced-folder path a liveness signal parallel to RemoteTest (write-a-marker/observe-mtime, or an explicit 'confirm your sync client shows this file as uploaded' step). Word the summary as 'placed inside <folder> — verify your sync client uploads it', never asserting it is synced. | durability-1 |
| C3 | Never silently repoint an existing named profile. Default ProfileName=basename and profile.Add replaces a same-name entry in place (profile.go:93-98). Before ProfileAdd, detect an existing same-name profile pointing at a DIFFERENT path and either refuse with a typed ErrProfileNameInUse (offering a suffixed name) or require explicit confirmation. | durability-3, failure-recoverability-5 |
| C4 | Surface the cross-version SeaVaultData preflight note. Add a preflight/compatibility note field to Result that Execute populates with vault.SyncClientPreflightNote when VaultDir is under a detected provider folder (init/handleInit emit this today), and broaden the segment matcher to prefix-match org-suffixed CloudStorage forms (OneDrive-*, GoogleDrive-*, Box-*) so it fires for the folders the wizard recommends. | durability-4, migration-coexistence-3 |
| C5 | Let a custom sync-folder path reach the 'already synced' (no-transport) outcome. Add a manual 'my vault is inside a folder my own sync client already watches' choice at step 4, or infer synced-folder mode when the chosen path sits under any detected root even when the default was overridden, so a custom path is not mislabeled local-only. | usability-friction-3 |
| C6 | De-risk the mandatory 52-char no-paste recovery re-type on the golden path within U1: make the recovery step visibly optional and reversible ('set this up now' vs 'remind me after my first successful file', with a consequence line), and let the user download/print the shown phrase before the re-type gate so the re-type verifies a stored copy, not working memory. Keep paste-blocking. | usability-friction-4 |
| C7 | Establish one source of truth for provider caveats. docs/cloud-provider-notes.md contains no caveat text (verified: grep returns nothing) yet §4/§8/T7 claim the Notes are 'lifted from' it. Either author a per-provider caveat catalog in that doc and have DetectSyncFolders read it (so T1/T7 assert the real doc string), or drop the 'lifted from docs' framing and state the caveats live in internal/setup; point T1/T7 at the real single source. Give box/pcloud/mega real caveats or remove them from the Provider enum. | integration-seams-6, dependency-cost-1, honesty-of-claims-3 |
| C8 | Make partial-create recoverable. CreateWithOptions MkdirAll's the SeaVaultData layout (vault.go:282,285) before the KDF wrap and config write (vault.go:339), so a kill/error between leaves a non-empty dir with no vault.json; create is then refused with ErrVaultDirNotEmpty while 'open it instead' opens nothing. Build the vault in a sibling temp dir and os.Rename on success, or RemoveAll on error; define ErrVaultDirNotEmpty by vault.json presence; make 'open it instead' fall back to 'leftover files from an interrupted setup — remove them and retry?' when no config is present. | failure-recoverability-1 |
| C9 | Branch the retry remedy by which step failed. §6 prints `seavault remote test NAME` for both rclone-install and RemoteTest failures, but after an install failure the remote was never added (remotes.Get 'not found') and there is no managed binary (BinaryPath fails). An install/RcloneEnsure failure must print `seavault rclone install` (with offline-archive/from-binary options); only a RemoteTest failure on an installed runtime prints `seavault remote test NAME`. CloudNote carries the step-appropriate command from Execute's partial Result. | failure-recoverability-6, dependency-cost-3 |
| C10 | Ship one OS-path detector, not two. DetectSyncFolders duplicates userpath.SuggestedVaultPaths and already disagrees with it (iCloud, Google Drive paths). Make DetectSyncFolders the single detector and have SuggestedVaultPaths delegate to it (or vice versa) under one T1 table; a test asserts they never diverge. | dependency-cost-2 |
| C11 | Name RcloneEnsure as new work and get consent for its network fetch. No install-if-missing+verify function exists (rclonebin has Install/Status/VerifyRuntime only); on the online default it downloads version.txt, a ~512MB-capped zip, and SHA256SUMS from downloads.rclone.org, and --preset rclone does so with no consent. Give RcloneEnsure its own tests, offer offline-archive/from-binary install options inline when the online fetch is refused, and require an explicit acknowledgement (not a silent download) on the non-interactive --preset rclone path. | dependency-cost-4 |
| C12 | Correct T6's expected status to 403. serveNoSession answers a no-session /api/* request with 403 (server.go:742); 401 is the session-present-not-logged-in path (server.go:769). Change T6 to assert 403 for the no-session case, assert 401 only for the login-required path if in scope, and fix the I-S7 prose to name the branch it hits. | honesty-of-claims-2 |
| C13 | Prove I-S1's 'never logged / never in any response' clauses. No cited row reads the /api/setup/run response body or a log sink. Add an assertion that decodes both the success and error /api/setup/run bodies and fails if the password substring appears, plus a captured-log check in T2/T3; or narrow I-S1's proven scope in §8 to the argv+stdout+summary clauses and label the response/log clauses covered-by-review. | honesty-of-claims-4 |
| C14 | Reconcile §5 and §8 and give I-S4 a test. §5 says every unlabeled invariant is proven; §8 omits I-S4 and I-S6. Add a row asserting the keychain prompt is presented with defaultYes=true (scripted prompter records the Confirm call and its default) and add I-S6 (T7) explicitly to §8, or drop the §5 'proven unless labeled' blanket and let §8 be the single authoritative proven-list. | honesty-of-claims-5 |

**Stats:** 49 findings judged, 28 refuted by the skeptic pass, 21 surviving, 0 blockers, 0 rescued. All 60 agents: claude-opus-4-8.

Applied as design revision 2 in `docs/design-setup-wizard.md` §9.
